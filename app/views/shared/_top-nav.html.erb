<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <%= link_to app_title, recognitions_path, class: 'navbar-brand mb-0 h1' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if logged_in? %>
	      <li class="nav-item">
	        <%= link_to 'Home', recognitions_path, class: 'nav-link' %>
	      </li>
        <li class="nav-item">
          <%= link_to 'New', new_recognition_path, class: 'nav-link' %>
        </li>
	      <li class="nav-item">
	        <%= link_to 'Stats', statistics_path, class: 'nav-link' %>
	      </li>
      <% end %>
    </ul>
    <div class="form-inline my-2 my-lg-0">
      <% if logged_in? %>
        <%= link_to 'Sign out', signout_path, class: 'btn btn-outline-light my-2 my-sm-0' %>
      <% else %>
        <%= link_to 'Sign in', signin_path, class: 'btn btn-outline-light my-2 my-sm-0' %>
      <% end %>
    </div>
  </div>
</nav>
